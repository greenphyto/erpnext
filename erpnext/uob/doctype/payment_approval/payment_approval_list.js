frappe.listview_settings['Payment Approval'] = {
    onload(listview) {
        console.log(4444, listview)
        // var action = ()=>{
        //     frappe.set_route("payment-bulk-approval")
        // }
        // listview.page.add_view_to_menu("Bulk Approval", action)
    }
};
